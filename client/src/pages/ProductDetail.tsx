import { Button } from "@/components/ui/button";
import { StoreHeader } from "@/components/StoreHeader";
import { trpc } from "@/lib/trpc";
import { addCartItem, readCart, writeCart } from "@/lib/cart";
import { ArrowLeft, BadgeCheck, Box, Clock3, Loader2, Server, ShieldCheck, ShoppingBag } from "lucide-react";
import { Link, useRoute } from "wouter";
import { toast } from "sonner";

const money = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });

export default function ProductDetail() {
  const [, params] = useRoute("/products/:slug");
  const product = trpc.catalog.product.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) });
  const destinations = trpc.catalog.productServers.useQuery({ productId: product.data?.id ?? 0 }, { enabled: Boolean(product.data?.id) });

  function addToCart() {
    const destination = destinations.data?.[0];
    if (!product.data || !destination) return toast.error("Este produto não possui um destino disponível.");
    const updated = addCartItem(readCart(), { ...product.data, featured: false }, destination.id, destination.name);
    writeCart(updated);
    toast.success("Produto adicionado ao carrinho. Volte à loja para finalizar.");
  }

  if (product.isLoading) return <main className="account-page grid min-h-screen place-items-center"><Loader2 className="animate-spin text-emerald-300" /></main>;
  if (product.isError) return <main className="account-page grid min-h-screen place-items-center p-6 text-center text-white"><div className="account-guard"><div className="status-orb mx-auto"><Box size={21} /></div><h1 className="font-display mt-5 text-2xl font-bold">Não foi possível carregar este benefício</h1><p className="mt-2 text-sm leading-6 text-slate-400">Verifique a conexão e tente abrir a recompensa novamente.</p><Button onClick={() => product.refetch()} className="mt-6 bg-emerald-300 text-slate-950 hover:bg-emerald-200">Tentar novamente</Button></div></main>;
  if (!product.data) return <main className="account-page grid min-h-screen place-items-center p-6 text-center text-white"><div className="account-guard"><div className="status-orb mx-auto"><Box size={21} /></div><h1 className="font-display mt-5 text-2xl font-bold">Benefício não encontrado</h1><p className="mt-2 text-sm leading-6 text-slate-400">Este item pode ter sido removido ou não estar disponível neste momento.</p><Link href="/" className="mt-6 inline-flex text-sm font-bold text-emerald-300">Voltar à loja</Link></div></main>;

	  return <div className="minecraft-store account-page product-detail-page text-slate-100"><StoreHeader /><main className="minecraft-detail-page relative px-5 py-8 sm:p-12"><div className="mx-auto max-w-6xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-400 hover:text-emerald-200"><ArrowLeft size={16} /> Voltar aos benefícios</Link><section className="minecraft-detail-shell product-detail-shell mt-7 grid gap-8 overflow-hidden p-4 sm:p-6 lg:grid-cols-[.88fr_1.12fr] lg:p-8"><div className="minecraft-frame product-detail-visual relative grid min-h-[18rem] place-items-center overflow-hidden sm:min-h-[26rem]">{product.data.imageUrl ? <img src={product.data.imageUrl} alt="" className="absolute inset-0 h-full w-full object-cover" /> : <div className="grid h-32 w-32 place-items-center rounded-[2rem] border border-white/20 bg-gradient-to-br from-emerald-200 to-cyan-300 text-slate-950 shadow-[0_0_60px_rgba(52,211,153,.25)]"><Box size={55} /></div>}<div className="absolute inset-0 bg-gradient-to-t from-[#07111d] via-[#07111d]/20 to-transparent" /><span className="minecraft-kicker absolute left-5 top-5 border border-white/15 bg-[#07111d]/70 px-3 py-1.5 text-[9px] font-bold tracking-[.14em] text-emerald-100 backdrop-blur">ITEM DIGITAL</span><div className="minecraft-info-strip absolute bottom-5 left-5 right-5 flex items-center gap-3 p-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-300/10 text-emerald-200"><BadgeCheck size={18} /></div><p className="text-xs leading-5 text-slate-200">Benefício preparado para a sua conta no servidor selecionado.</p></div></div><div className="flex flex-col justify-center p-2 sm:p-4"><p className="section-kicker">{product.data.categoryName.toUpperCase()}</p><h1 className="minecraft-heading font-display mt-3 text-4xl font-extrabold leading-tight text-white sm:text-5xl">{product.data.name}</h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-300">{product.data.description || product.data.shortDescription || "Benefício digital configurado para ser entregue com segurança no servidor selecionado."}</p>{destinations.isError ? <p className="mt-5 rounded-xl border border-rose-300/20 bg-rose-300/10 px-4 py-3 text-sm text-rose-100">Os destinos deste produto não puderam ser carregados. Tente novamente antes de comprar.</p> : <div className="mt-7 grid gap-3 sm:grid-cols-2"><div className="product-detail-fact"><Clock3 size={17} /><div><span>Duração</span><strong>{product.data.durationDays ? `${product.data.durationDays} dias` : "Permanente"}</strong></div></div><div className="product-detail-fact"><Server size={17} /><div><span>Destino</span><strong>{destinations.data?.map(server => server.name).join(", ") || "Carregando…"}</strong></div></div></div>}<div className="minecraft-info-strip mt-7 p-4"><div className="flex items-start gap-3"><ShieldCheck className="mt-0.5 shrink-0 text-emerald-300" size={18} /><p className="text-sm leading-6 text-emerald-50">Após a confirmação pelo gateway, a entrega é processada com segurança para o jogador informado no checkout.</p></div></div><div className="mt-8 flex flex-wrap items-center justify-between gap-5 border-t border-white/10 pt-6"><div><p className="text-xs font-semibold tracking-[.12em] text-slate-500">VALOR DO BENEFÍCIO</p><strong className="mt-1 block font-mono text-3xl text-emerald-200">{money.format(product.data.priceCents / 100)}</strong></div><Button disabled={destinations.isLoading || destinations.isError || !destinations.data?.length} onClick={addToCart} className="minecraft-cta h-12 px-6 font-bold text-slate-950"><ShoppingBag size={18} /> Adicionar ao carrinho</Button></div></div></section></div></main></div>;
}
